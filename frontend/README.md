
  # Agricultural Yield Prediction App

  This is a code bundle for Agricultural Yield Prediction App. The original project is available at https://www.figma.com/design/BOTKMtW8McRIzcidzBvyka/Agricultural-Yield-Prediction-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  